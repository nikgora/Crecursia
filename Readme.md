Завдання з теми Рекурсія

1. Описати рекурсивну функцію NOD(A, B) цілого типу, що знаходить
   найбільший спільний дільник (НОД) двох цілих позитивних чисел A і B,
   використовуючи удосконалений алгоритм Евкліда.
   За допомогою цієї функції знайти НОД(A, B), НОД(A, C), НОД(A, D), якщо
   дані числа A, B, C, D.
2. Функція Маккарті визначена так:

Маккарті(n)=
Знайти значення функції Маккарті при заданому n.

3. Написати рекурсивну функцію визначення кількості заданих літер у
   вхідному тексті. Вивести вхідний текст у прямому та зворотному порядку,
   використовуючи рекурсивні функції.
4. Обчислити значення функції f(x) на інтервалі [a,b]. Функцію f(x) визначає
   користувач.